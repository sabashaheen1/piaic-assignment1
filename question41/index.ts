
  function show_magicians(magicians: string[]) {
    for (let i = 0; i < magicians.length; i++) {
      console.log(magicians[i]);
    }
  }

  let magicianNames: string[] = ["Mystica", "Enigma", "Zephyr", "Astral"];

  show_magicians(magicianNames);